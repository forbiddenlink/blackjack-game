{
  "name": "blackjack-game",
  "version": "2.0.0",
  "description": "Portfolio-grade blackjack simulator built with vanilla HTML, CSS, and JavaScript.",
  "main": "index.js",
  "scripts": {
    "start": "http-server -c-1 .",
    "test": "node --test tests/*.test.js && node --check blackjack-core.js && node --check index.js && node --check posthog.js",
    "test:watch": "node --test --watch tests/*.test.js",
    "build": "echo \"Static project: no build step required.\"",
    "check": "pnpm biome:check && pnpm test && pnpm build",
    "security": "pnpm audit --audit-level high",
    "audit": "pnpm audit --audit-level high",
    "biome:check": "biome check .",
    "biome:fix": "biome check . --write",
    "biome:format": "biome format . --write"
  },
  "keywords": [
    "blackjack",
    "javascript",
    "portfolio",
    "web-game"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "http-server": "14.1.1"
  },
  "packageManager": "pnpm@10.34.5",
  "pnpm": {
    "overrides": {
      "follow-redirects": ">=1.16.0 <2",
      "qs": ">=6.15.3 <7"
    }
  }
}
